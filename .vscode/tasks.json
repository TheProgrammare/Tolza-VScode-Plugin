{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Tolza VSIX",
            "type": "shell",
            "command": "npx vsce package",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "dependsOn": "npm: compile",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}